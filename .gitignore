*.AppImage
data/
dist-server/
dist-web/
.env
*.log
node_modules/
release/
*.tar.gz
work/

# O componente nativo do Windows é compilado, não versionado.
native/windows/audio-helper/build/
dist-updates/

# A origem e as chaves de UMA build. Descrevem a máquina que empacotou, não o
# projeto — o repositório não traz o domínio de ninguém.
desktop/update-origin.generated.cjs
